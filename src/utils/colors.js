/**
 * Global Category Color System Tokens
 * Centrally manages unified color hex codes across all visual UI components,
 * including donut charts, bar graphs, and tabular logs.
 */
export const categoryColors = {
  Food: "#E53E3E",          // Deep Red Accent
  Entertainment: "#4A90D9", // Clean Blue Accent
  Utilities: "#F6AD55",     // Vibrant Amber Accent
  Transport: "#38B2AC"      // Fresh Teal Accent
};

/**
 * Fallback Utility Color
 * Used when a custom or dynamic category does not explicitly map to the tokens above.
 */
export const defaultColor = "#71717a"; // Muted Gray Zinc